<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="wrap_content"
    android:gravity="center">

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@drawable/im_progress_dialog_bg"
        android:clickable="true"
        android:focusable="true"
        android:focusableInTouchMode="true"
        android:gravity="center"
        android:minHeight="@dimen/im_NormalListHeight"
        android:padding="@dimen/im_NormalPadding">

        <ProgressBar
            android:id="@id/im_progress_dialog_icon"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:indeterminateDrawable="@drawable/progress_medium_holo" />

        <TextView
            android:id="@id/im_progress_dialog_msg"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:maxWidth="200.0dip"
            android:minWidth="150.0dip"
            android:textColor="@color/white"
            android:textSize="@dimen/im_NormalTextSize" />
    </LinearLayout>

</LinearLayout>